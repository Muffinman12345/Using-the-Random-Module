#Import the random module (REQUIRED)

import random

#Make a random variable

myRandomVariable = random.randint(1, 100)

#1 is the smallest possible number. The 100 is the largest possible number. This will result in making a variable that is a random number from 1 through 100
