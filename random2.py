#Import modules
import random

#Create random variable
myVariable = random.random(1, 100)
#This return a random number (like 3.1415) from 1 through 100
#Check out the other module to learn to just get int's (numbers with no decimals)
